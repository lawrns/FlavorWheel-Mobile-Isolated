import {
  describe,
  it,
  test,
  expect,
  beforeEach,
  afterEach,
  beforeAll,
  afterAll,
  vi,
} from 'vitest'

export { describe, it, test, expect, beforeEach, afterEach, beforeAll, afterAll }

// Provide a Jest-compatible named export
export const jest = vi

